# BND877M Metallic Map Prompts

Use the attached BND877M bottle image as the exact base reference. Keep the product position, scale, crop, canvas size, perspective, silhouette, and all part boundaries perfectly identical to the input image. Do not rotate, resize, recenter, redraw, stylize, or change the bottle geometry. Output a clean PNG map for technical mockup compositing. No text, no logo, no watermark, no extra objects.

Create `BND877M_metallic_highlight.png`.

Purpose: this is a grayscale highlight map used only to add general light and broad metallic light response to the recolored bottle.

Using the attached BND877M bottle as the exact base, generate a grayscale technical map. Pure black means no highlight. White means strongest broad metallic light. Include soft but clearer metallic light bands on the bottle body, cap, neck transition, and ring handle. Preserve the original light placement from the source image, but keep the response satin-metallic rather than chrome or mirror-like. The highlights should feel cleaner and slightly firmer than matte, but still broad enough for a product mockup renderer.

Do not include dark shadows, ambient occlusion, edge darkening, texture noise, mirror reflections, or product color. Background should be pure black. Keep the bottle geometry, seams, ring handle position, and alignment exactly identical to the source image.

/

Use the attached BND877M bottle image as the exact base reference. Keep the product position, scale, crop, canvas size, perspective, silhouette, and all part boundaries perfectly identical to the input image. Do not rotate, resize, recenter, redraw, stylize, or change the bottle geometry. Output a clean PNG map for technical mockup compositing. No text, no logo, no watermark, no extra objects.

Create `BND877M_metallic_shadow.png`.

Purpose: this is a grayscale shadow map used only to darken the recolored bottle and define metallic form.

Using the attached BND877M bottle as the exact base, generate a grayscale technical map. Pure black means no shadow. White means strongest shadow or darkening. Only include broad form shadows, cylindrical side falloff, handle interior shadow, neck and cap separation shadow, seam darkness, underside contact shadows, and the darker shaded side of the body. Keep the form readable and controlled so the metallic finish still feels solid and dimensional.

Do not include highlights, glossy reflections, bright streaks, texture speckles, or product color. The product area should contain only shadow information. Background should be pure black. Keep all bottle edges, seams, cap alignment, and handle position exactly matching the source image.

/

Use the attached BND877M bottle image as the exact base reference. Keep the product position, scale, crop, canvas size, perspective, silhouette, and all part boundaries perfectly identical to the input image. Do not rotate, resize, recenter, redraw, stylize, or change the bottle geometry. Output a clean PNG map for technical mockup compositing. No text, no logo, no watermark, no extra objects.

Create `BND877M_metallic_base.png`.

Purpose: this is the neutral metallic base or albedo image for a 2D product mockup renderer.

Using the attached BND877M bottle as the exact base, create a clean light metallic silver product base image. Preserve the exact silhouette, cap, bottle body, neck transitions, lower seam, and ring handle alignment. Keep the product neutral and bright, but remove strong shadows, deep ambient occlusion, harsh reflections, and dramatic contrast as much as possible. The result should look like a clean satin metallic base that can later receive Pantone color, shadows, highlights, texture, and specular maps.

Keep only gentle form shading so the object shape remains readable. Do not add color tint. Do not change the background. Do not add logo or text.

/

Use the attached BND877M bottle image as the exact base reference. Keep the product position, scale, crop, canvas size, perspective, silhouette, and all part boundaries perfectly identical to the input image. Do not rotate, resize, recenter, redraw, stylize, or change the bottle geometry. Output a clean PNG map for technical mockup compositing. No text, no logo, no watermark, no extra objects.

Create `BND877M_metallic_texture.png`.

Purpose: this is a grayscale texture map used only to add subtle metallic surface variation to the recolored bottle.

Using the attached BND877M bottle as the exact base, generate a grayscale technical map. Mid gray means neutral texture. Slightly lighter or darker values indicate subtle metallic surface variation. Include only fine satin-metal grain or soft brushed-metal irregularity across the bottle body, cap, and ring handle. Keep the texture low-contrast, clean, and even so it enhances realism without looking like dirt, scratches, or damaged metal.

Do not include shadows, highlights, ambient occlusion, seam darkening, glossy streaks, or product color. Background should be solid mid gray or pure black only if your workflow requires black-background maps, but the product area itself must carry only controlled texture information. Keep exact alignment with the source image.

/

Use the attached BND877M bottle image as the exact base reference. Keep the product position, scale, crop, canvas size, perspective, silhouette, and all part boundaries perfectly identical to the input image. Do not rotate, resize, recenter, redraw, stylize, or change the bottle geometry. Output a clean PNG map for technical mockup compositing. No text, no logo, no watermark, no extra objects.

Create `BND877M_metallic_specular.png`.

Purpose: this is a grayscale specular map used only for metallic surface sheen and controlled metallic reflections.

Using the attached BND877M bottle as the exact base, generate a grayscale technical map. Pure black means no specular response. White means strongest specular response. For this metallic finish, keep the specular map cleaner, brighter, and more focused than matte, but not mirror-like or chrome-like. Include soft metallic sheen, controlled bright response on curved surfaces, and slightly stronger light pickup on the bottle body, cap, neck transition, and ring handle.

Do not include diffuse body color, soft shadows, ambient occlusion, texture grain, seam darkness, or broad mirror reflections. Background should be pure black. Keep exact alignment with the source image.

/

Use the attached BND877M bottle image as the exact base reference. Keep the product position, scale, crop, canvas size, perspective, silhouette, and all part boundaries perfectly identical to the input image. Do not rotate, resize, recenter, redraw, stylize, or change the bottle geometry. Output a clean PNG map for technical mockup compositing. No text, no logo, no watermark, no extra objects.

Create `BND877M_metallic_edge_ao.png`.

Purpose: this is a grayscale edge and ambient occlusion map used only to add contact darkening and seam definition to the recolored bottle.

Using the attached BND877M bottle as the exact base, generate a grayscale technical map. Pure black means no edge darkening. White means strongest ambient occlusion and contact darkening. Include only seam darkness, cap-to-body contact lines, lower seam definition, handle connection shadows, inner handle contact darkening, and subtle edge occlusion where surfaces meet or overlap.

Do not include broad form shadows, highlights, metallic reflections, texture grain, or product color. Keep the map clean and controlled so it strengthens shape definition without making the bottle look dirty. Background should be pure black. Keep exact alignment with the source image.
